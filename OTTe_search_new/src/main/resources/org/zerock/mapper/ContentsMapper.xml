<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.zerock.mapper.ContentsMapper">

	<!-- id 속성은 DAO 의 메소드와 같아야 한다. 여기의 boardVO 는 mybatis-confg.xml 에 Alias 되어 
		있습니다. -->
	<select id="selectContentsAll" resultType="org.zerock.domain.ContentsVO">
		select
		cotents_id,nation_id,ott_id,grade_id,class_id,contents_title,cotents_summary,cotents_years,
		cotents_season,cotents_episode,cotents_score,cotents_duration
		from
		contents_t
		where contents_title = #{contents_title}
	</select>
</mapper>
